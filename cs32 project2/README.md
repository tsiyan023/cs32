# Project 2 Clean Code Package

This package contains the cleaned versions of the six files provided by the user.

Changes made:
- Removed C/C++ line comments (`// ...`).
- Removed C/C++ block comments (`/* ... */`).
- Preserved executable/source code.
- Renamed `main (2).cpp` to `main.cpp`.
- Left `report.docx` unchanged because it is not a source code file.
